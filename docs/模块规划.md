# Nebula 平台模块规划与当前边界

> 最后更新：2026-08-01
> 后端基线：`nebula@3d35d13ad23feb4ce367585b77de90094f3f2e26`
> 前端基线：`nebula-studio@5a36a7e09787889607d53ddea65b3e25b98b5397`
> 本文描述当前 Reactor 与代码边界，不把目标能力写成已实现能力。完成度与风险见 [当前实现状态分析](./nebula-current-state-analysis.md)。

## 1. 项目定位

Nebula 是基于 Spring Boot 的企业应用与集成运行平台；Nebula Studio 是其 Web/Electron 管理与开发体验。平台目标是复用 Spring、Camel、MyBatis、PF4J 等成熟生态，统一资源、治理、版本、发布、插件、任务、租户、安全和集成运行能力，而不是重新实现基础框架。

当前系统可划分为五层：

```text
体验层       Nebula Studio：Web Shell、Electron、Frontend、Integration、Settings、Login、Docs
应用层       platform-console、platform-integration、platform-integration-executor
领域层       resource、governance、release、version、task、tenant、security、system
运行层       runtime、camel、plugin、cluster、subscribe
基础设施层   database、integration、capability、config、BOM、tools
```

## 2. 技术基线

### 2.1 后端

| 技术 | 当前版本/实现 |
| --- | --- |
| Java | 25 |
| Spring Boot | 4.1.0 |
| Apache Camel | 4.20.0 |
| MyBatis / MyBatis-Plus | 3.5.16 / 3.5.16 |
| Flyway | 10.21.0 |
| PostgreSQL / MySQL Driver | 42.7.2 / 9.3.0 |
| Debezium | 3.0.7.Final |
| PF4J | 3.15.0 |
| JJWT | 0.13.0 |
| Redisson | 3.38.1 |
| AWS S3 SDK | 2.45.1 |

### 2.2 前端

| 技术 | 当前版本/实现 |
| --- | --- |
| Node.js | `>=22.12.0` |
| Vite+ / Vite | 0.2.6 / 8.1.3 |
| Vue / Vue Router | 3.5.35 / 4.6.4 |
| TypeScript | 6.0.3 |
| Electron / electron-vite | 43.x / 5.x |
| Tailwind CSS | 4.3.3 |
| Vitest / Playwright | 4.1.10 / 1.62.0 |

版本以 `nebula-bom/pom.xml` 和 `nebula-studio/pnpm-workspace.yaml` 为准。

## 3. 后端 Reactor

根 POM 当前聚合 22 个顶层条目（含 `demos`）。完整活动树如下；未列出的目录不属于当前 Reactor。

```text
nebula-bom
nebula-tools
nebula-runtime
├── runtime-core
├── runtime-context
├── runtime-lifecycle
├── runtime-event
├── runtime-extension
└── runtime-starter
nebula-resource
├── resource-core
├── resource-api
├── resource-registry
├── resource-query
└── resource-starter
nebula-governance
├── governance-core
├── governance-api
├── governance-request
├── governance-approval
├── governance-workflow
├── governance-policy
├── governance-audit
└── governance-starter
nebula-release
├── release-core
├── release-api
├── release-manager
└── release-starter
nebula-version-control
├── version-api
├── version-core
├── version-storage
└── version-starter
nebula-plugin
├── nebula-plugin-api
├── nebula-plugin-sdk
├── nebula-plugin-core
├── nebula-plugin-bom
├── nebula-plugin-starter
├── nebula-plugin-adaptor
└── nebula-plugin-repository
nebula-integration
├── integration-redis
├── integration-s3
├── integration-kafka
└── integration-mail
nebula-database
├── nebula-database-core
├── nebula-database-adapter
│   ├── nebula-database-adapter-core
│   ├── nebula-database-adapter-postgres
│   └── nebula-database-adapter-mysql
├── nebula-database-mybatis
├── nebula-database-jpa
└── nebula-database-starter
nebula-capability
├── capability-cache
├── capability-lock
├── capability-storage
├── capability-log
├── capability-audit
├── capability-message
├── capability-notification
├── capability-websocket
└── capability-encrypt
nebula-config
├── config-core
├── config-model
├── config-storage
├── config-center-api
├── config-center-spring-cloud
├── config-center-nacos
├── config-runtime
├── config-autoconfigure
└── config-starter
nebula-task
├── task-core
├── task-model
├── task-trigger
├── task-scheduler
├── task-executor
├── task-dependency
├── task-cluster
└── task-starter
nebula-tenant
├── tenant-core
├── tenant-context
├── tenant-storage
└── tenant-starter
nebula-security
├── nebula-security-domain
├── nebula-security-web
├── nebula-security-session
├── nebula-security-token
├── security-core
├── security-context
├── security-authorization
├── security-oauth2
└── security-starter
nebula-cluster
├── cluster-core
├── cluster-discovery
├── cluster-lock
├── cluster-event
└── cluster-starter
nebula-subscribe
├── subscribe-core
├── subscribe-api
├── subscribe-delivery
└── subscribe-starter
nebula-camel
├── nebula-camel-core
├── nebula-camel-starter
├── nebula-camel-plugin-api
├── nebula-camel-plugin-sdk
├── nebula-camel-subscribe
├── nebula-camel-tenant
├── nebula-camel-plugin
├── nebula-camel-security
├── nebula-camel-monitor
├── nebula-camel-console
├── nebula-camel-executor
├── nebula-camel-console-starter
├── nebula-camel-executor-starter
├── nebula-camel-message
├── nebula-camel-metadata
├── nebula-camel-builtin-plugins
│   ├── nebula-camel-builtin-plugin-mysql
│   ├── nebula-camel-builtin-plugin-postgre
│   └── nebula-camel-builtin-plugin-http
├── nebula-camel-runtime
├── nebula-camel-route
├── nebula-camel-dag
├── nebula-camel-sql
├── nebula-camel-trigger
└── nebula-camel-observability
nebula-system
├── nebula-system-app
├── nebula-system-organization
├── nebula-system-user
├── nebula-system-user-organization
├── nebula-system-config
├── nebula-system-log
└── nebula-system-starter
nebula-modules
├── nebula-module-file
├── nebula-module-user
├── nebula-module-organization
├── nebula-module-config
└── nebula-module-tenant-extension
nebula-platform
├── platform-console
├── platform-integration
└── platform-integration-executor
demos
├── demo-plugins/demo-plugin-hello
├── demo-starter
├── demo-platform
├── demo-camel-plugin-examples
│   ├── demo-camel-plugin-example-database
│   └── demo-camel-plugin-example-protocol
├── demo-camel-console
└── demo-camel-executor
```

`nebula-platform/platform-admin` 有被 Git 跟踪的残留 POM 和源码，但不在 `nebula-platform` 聚合 POM 中，因此不是活动 Reactor 模块。它不得出现在正式部署拓扑中，后续应归档或删除。

## 4. 后端模块职责与边界

### 4.1 构建与运行时基础

| 模块 | 当前职责 | 边界 |
| --- | --- | --- |
| `nebula-bom` | Java 与三方依赖版本、插件管理 | 不包含业务实现 |
| `nebula-tools` | 构建/开发工具 | 不参与运行时领域模型 |
| `nebula-runtime` | 生命周期、上下文、事件、扩展点 | 不直接承担资源治理或 Camel 业务 |
| `nebula-cluster` | 节点、发现、锁与集群事件 | 当前仍需分布式接管验收 |

### 4.2 资源治理主链

| 模块 | 当前职责 | 当前限制 |
| --- | --- | --- |
| `nebula-resource` | 资源类型、实例、Registry 和查询 | 各领域 registrar 覆盖仍需扩展 |
| `nebula-governance` | 申请、审批、策略、工作流与审计 | 已有服务/API，真实发布链待闭环 |
| `nebula-version-control` | 快照、版本和 diff | diff 仍需结构化增强 |
| `nebula-release` | 发布、部署、回滚抽象 | 默认 LocalDeployTarget 仅写日志 |

正确调用方向：

```text
Resource Registry
  → Governance Request/Approval
  → Version Snapshot
  → Release
  → Domain DeployTarget
  → Runtime/Executor
```

Release 不能反向承载审批规则，Governance 不能直接操作 Camel Runtime，领域部署由 `DeployTarget` 适配。

### 4.3 插件与 Camel

#### 平台插件

`nebula-plugin` 管理 PF4J 生命周期、descriptor、SDK、安装和仓库。当前真实 Maven 远程下载、搜索、SHA-256 校验与本地索引已实现；剩余重点是签名、可信供应商和依赖约束。

#### Camel 插件与执行

`nebula-camel` 在平台插件之上提供 Camel 领域能力：Connector、Route、DAG、SQL、Trigger、订阅、Gateway、监控和 Console/Executor 装配。平台 descriptor 与 Camel descriptor 不应再次合并为一个大接口。

内置 MySQL、PostgreSQL、HTTP 插件是当前 Reactor 的真实子模块；demo 插件只是 SDK 示例。

### 4.4 数据、集成与通用能力

| 模块 | 职责 |
| --- | --- |
| `nebula-database` | 多数据源、MyBatis/JPA、Flyway、PostgreSQL/MySQL 元数据适配 |
| `nebula-integration` | Redis、S3、Kafka、Mail 客户端与健康检查 |
| `nebula-capability` | 缓存、锁、存储、日志、审计、消息、通知、WebSocket、加密能力 |
| `nebula-config` | basic/center 配置加载、存储、快照、刷新、自动配置 |

`nebula-integration` 是外部客户端适配，`nebula-capability` 是上层可消费的业务无关能力。两者不能仅按三方依赖重复实现同一接口。

### 4.5 身份、租户与系统

| 模块 | 职责 | 当前限制 |
| --- | --- | --- |
| `nebula-security` | JWT、Session、RBAC、Web 安全及可选 OAuth2 子模块 | OAuth2 未进入现行平台装配；MFA/TOTP/恢复未实现；Token 撤销仍为进程内状态 |
| `nebula-tenant` | TenantId、上下文、存储和 starter | SQL 拦截器只检查，不强制注入 |
| `nebula-system` | 应用、组织、用户、用户组织、配置、日志 | 与租户/认证的持久绑定需真实栈验证 |
| `nebula-modules` | 面向平台的文件、用户、组织、配置等组合模块 | 避免与 `nebula-system` 继续重复扩张 |

长期边界建议：

- security 负责认证凭证、会话和授权判断；
- system 负责平台身份实体；
- tenant 负责数据与执行上下文隔离；
- modules 只提供应用层组合，不重新定义领域模型。

### 4.6 任务与订阅

`nebula-task` 已拆分模型、Trigger、Scheduler、Executor、Dependency、Cluster 和 starter；`nebula-subscribe` 负责订阅定义和交付；`nebula-camel-subscribe` 负责 Camel/POLLING/CDC 适配。

剩余边界：

- Task Dependency 必须进入 Scheduler 主路径；
- Task Cluster 必须使用共享节点/锁，而不是仅本地算法；
- 通用 Subscribe 与 Camel Subscribe 共用平台模型，领域模块只维护协议适配；
- CDC 生产模式禁止未授权模拟，并提供共享 offset 与单活接管。

## 5. 正式应用拓扑

### 5.1 三平台应用

| 应用 | 端口 | 组合职责 |
| --- | ---: | --- |
| `platform-console` | 8090 | System、Resource、Governance、Config、Version、Release、Task、Plugin、OpenAPI |
| `platform-integration` | 8080 | Camel 定义面、认证、租户、订阅和治理入口 |
| `platform-integration-executor` | 8081 | Route/Gateway/DAG/任务执行与监控 |

正式联调以这三个应用为准。`demo-camel-console` 和 `demo-camel-executor` 保留为示例，不能替代生产组合验收。

### 5.2 数据与调用路径

```text
Nebula Studio
├── /api/platform、/api/system、治理/版本/发布 → platform-console:8090
├── 默认 /api、认证、订阅、Camel Console     → platform-integration:8080
└── /api/executor、Gateway、运行监控          → platform-integration-executor:8081

三应用
├── PostgreSQL：持久化与 Flyway
├── Redis：缓存、锁与会话相关能力
├── Kafka：事件发布/后续消费链
├── S3：对象存储
└── Mail：通知通道
```

当前最后一次真实栈在 8090 的 ConfigService 装配处失败，因此该图是代码组合与目标运行拓扑，不是“当前已持续运行”的声明。

## 6. 前端模块规划

### 6.1 应用边界

| 应用 | 职责 |
| --- | --- |
| `apps/web` | 浏览器主 Shell 与 embed 入口 |
| `apps/electron` | 主进程、窗口生命周期和 renderer 引导 |
| `apps/electron-preload` | capability 驱动的安全桥接 |
| `sub-web/frontend` | 个人工作台和 Shell 组合 |
| `sub-web/integration` | Portal、Provider、Admin 与 Camel 业务 |
| `sub-web/settings` | 个人、组织、平台设置 |
| `sub-web/login` | AuthFlow |
| `sub-web/docs` | 产品帮助与开发者参考 |

### 6.2 共享边界

- `contracts`：服务端契约 facade，不包含页面 ViewModel。
- `api-client`：请求、认证/租户头、错误处理和响应解析。
- `app-shell`：窗口、嵌入协议、事件和宿主 SDK。
- `nebula-shell` / `nebula-layout`：体验容器与布局，不访问业务 API。
- `runtime`：子应用运行模式与启动编排。
- `editors`：可独立加载的编辑器能力。
- `features`：只接收有真实复用证据的完整业务切片；当前只有 `use-confirm`。

`configs/windows.json` 是窗口、API target、角色、help key 和 preload capability 的单源，Web 与 Electron 不得各自维护副本。

## 7. 依赖规则

### 7.1 后端

1. `*-core` 不依赖 Spring Web 和平台应用。
2. `*-api` 只暴露稳定契约，不泄漏内部存储模型。
3. `*-starter` 负责条件装配，不包含业务流程。
4. `platform-*` 只做组合和运行配置，不复制领域实现。
5. `demos` 不被生产应用依赖。
6. 领域间通过 API/事件/扩展点协作，禁止直接访问对方 Repository。
7. tenant/security/audit 上下文必须跨异步与 Camel 边界传播并清理。

### 7.2 前端

1. 应用可以依赖 core/ui/editors/contracts，反向依赖禁止。
2. 页面只组合 feature 和 ViewModel，不直接散布 fetch/DTO 转换。
3. UI 包不得依赖 Router、业务 API 或应用 Session。
4. shared feature 必须有两个以上消费者、稳定入口和独立测试。
5. 新 API 通过 generated contracts facade，禁止重复手写 DTO。
6. Electron/Web 共用配置与认证语义；差异仅留在 host adapter。

## 8. 当前模块级缺口

| 优先级 | 模块/边界 | 缺口 |
| --- | --- | --- |
| P0 | config + platform-console | 历史实跑缺少 ConfigService；当前源码有候选装配，需 Context/启动复验后才能判定 |
| P0 | release + runtime/camel | 默认 DeployTarget 未驱动真实运行时 |
| P0 | frontend contracts | generated facade 尚未覆盖全部业务消费 |
| P1 | tenant + database | tenant_id 未由统一机制强制 |
| P1 | task + cluster | DAG、唯一调度和故障接管未闭环 |
| P1 | camel subscribe | 共享 offset、单活和接管未生产化 |
| P1 | security + system | MFA/恢复与持久组织/租户认证链缺失 |
| P1 | Studio Integration | app-local feature 需继续收敛，按真实复用提升 |
| P2 | capability message/notification/websocket | DLQ、持久化和跨实例投递不足 |
| P2 | capability storage + integration-s3 | 本地/S3 按 `storageType` 的装配选择仍需专项验证 |
| P2 | camel monitor/observability | 进程内状态需迁移到统一可观测后端 |
| P2 | plugin repository | 签名、可信供应商、依赖约束和企业仓库适配 |

实施顺序和完成定义见 [详细开发计划](./nebula-development-detailed-plan.md)。

## 9. 模块变更准入

新增模块前必须回答：

1. 现有模块为何不能承载该职责？
2. 是否有独立生命周期、稳定 API 或至少两个消费者？
3. 是否会形成与 system/modules、integration/capability、runtime/camel 的重复？
4. 是否同时提供 POM/workspace 声明、测试、README 和运行装配？
5. 是否进入正式 Reactor/manifest？若未进入，必须标注实验或归档状态。

禁止：

- 只创建目录/POM 而无实现；
- 把 demo 作为正式依赖；
- 为满足规划数量创建空 feature；
- 在平台应用复制领域实现；
- 把未来接口写成当前能力。

## 10. 文档职责

- 本文：模块树、职责和依赖边界。
- [当前实现状态分析](./nebula-current-state-analysis.md)：完成度、证据、缺口和风险。
- [详细开发计划](./nebula-development-detailed-plan.md)：波次、任务与验收关口。
- [前端增量重构计划](./nebula-studio-frontend-refactoring-plan.md)：Studio 专项。
- `../nebula/docs/` 与 `../nebula-studio/docs/`：单仓库运行、测试和 API 明细。
