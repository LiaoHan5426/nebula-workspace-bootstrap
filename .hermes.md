# Nebula Workspace Hermes 入口

本仓库是协调 `nebula`、`nebula-studio`、中央知识库和跨仓库文档的多仓库工作空间。涉及两个源码仓库、真实栈、契约、E2E、架构或根目录文档的任务，按本文件执行。

## 开始任何任务前

1. 读取根目录 `README.md`，并按任务范围主动读取子仓库上下文：
   - 后端或 Platform：`nebula/.hermes.md` 与 `nebula/AGENTS.md`；
   - 前端、契约或 E2E：`nebula-studio/.hermes.md` 与 `nebula-studio/AGENTS.md`；
   - 跨仓库任务必须读取两组上下文，不因当前命令的 `workdir` 改变而省略。
2. 定位中央知识库，按顺序使用第一个存在的目录：
   - 环境变量 `WIKI_PATH`；
   - 工作区相对路径 `.knowledge/agent-memory`。
3. 根据任务读取最小必要知识：
   - 跨仓库关系：`projects/nebula-ecosystem.md`；
   - 后端：`projects/LiaoHan5426--nebula/index.md` 与 `module-map.md`；
   - 前端：`projects/LiaoHan5426--nebula-studio/index.md` 与 `workspace-map.md`；
   - 再按首页链接读取相关架构、开发或故障页面，不一次性加载整个知识库。
4. 在信任叙述知识前，执行知识库的 `scripts/project_navigation.py check --workspace-root <workspace-root>`。过期页面只能用于导航，所有结论必须回到当前源码、构建描述符和运行结果核验。

## 结构与影响分析

- 模块和 package 定位优先使用知识库生成的 Markdown 地图及 YAML manifest。
- 调用关系、依赖、变更影响、受影响执行流和跨仓库检索，优先调用 Hermes 原生 MCP 暴露的 `code-review-graph` 工具。
- Workspace 本地 `.venv` 中的 code-review-graph 是 Hermes MCP Server 的实现；各源码仓库中的 `.code-review-graph` 仅是可重建的仓库图数据，不是另一套权威知识库。
- 使用代码图前确认 MCP 可连接、两个源码仓库均已注册且图与当前源码一致；图过期时先增量更新。仍不可用时回退到 Git 跟踪文件和源码静态检查，并明确记录未执行结构化影响分析。

## 修改与验证闭环

1. 修改前记录涉及仓库的分支、HEAD 和工作树状态，并用知识页与代码图界定预计调整范围和最小测试集。
2. 修改中遵循对应子仓库上下文、项目 Skills 和测试要求；跨前后端契约变更必须同时核对生成流程、调用方和真实栈 E2E。
3. 修改后更新受影响仓库的代码图并检查影响范围，运行目标测试及必要的跨仓库验收。
4. 若模块、package、应用入口或内部依赖发生变化，运行知识库 `project_navigation.py refresh`，不得手工修改生成的 map/manifest。
5. 若架构边界、启动拓扑、契约、开发流程、运维方式或已知故障发生变化，更新对应人工维护知识页和 `projects/nebula-ecosystem.md`；仅实现细节变化且知识仍准确时，不为追平提交号而虚假重写内容，但必须按知识库规则更新或明确记录新鲜度结论。
6. 完成前运行知识库 lint、测试和 freshness check；报告源码仓库、控制仓库与知识库各自的修改和验证状态，不把 CRG 数据库、向量索引、缓存或密钥提交到 Git。

## 文档归属与可移植性

- 跨仓库规划与真实栈文档放在根 `docs/`，跨仓库架构放在根 `architecture/`。
- 后端专属内容归 `nebula`，前端专属内容归 `nebula-studio`，可复用项目知识归中央知识库。
- 不在版本控制文件中写入盘符、用户名、密钥或某台机器的绝对工具路径。
- 新机器或现有工作空间升级统一使用 `workspace.ps1 init|update` 或 `workspace.sh init|update`，由控制仓库维护知识库克隆、模块清单、代码图和 Hermes MCP 注册。
